{% extends 'Front/home.html.twig' %}

{% block main %}

	{{ form_start(userForm) }}

	{{ form_label(userForm.email, 'Votre email') }}
	{{ form_widget(userForm.email) }}

	{{ form_label(userForm.password, 'Votre password') }}
	{{ form_widget(userForm.password) }}

	{{ form_widget(userForm.submit) }}


	{{ form_end(userForm) }}

{% endblock %}
