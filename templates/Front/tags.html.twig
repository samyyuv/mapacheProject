{% extends 'Front/home.html.twig' %}

{% block main %}
	{% for tag in tags %}
		<h1>{{ tag.name }}</h1>
		<p>{{ tag.description }}</p>
		{% for article in tag.articles %}

			<h3>{{ article.title }}</h3>

		{% endfor %}
	{% endfor %}
{% endblock %}
